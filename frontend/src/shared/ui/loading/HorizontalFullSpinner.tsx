import Spinner from "./Spinner";

const HorizontalFullSpinner = () => {
  return (
    <div className="w-full flex items-center justify-center py-5">
      <Spinner />
    </div>
  );
};

export default HorizontalFullSpinner;
